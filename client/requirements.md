## Packages
framer-motion | Complex animations for player transitions and visualizers
lucide-react | Iconography for player controls
howler | Audio handling and playback control
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
Music files are served from /music/
Static images (logo/icon) are served from /images/
